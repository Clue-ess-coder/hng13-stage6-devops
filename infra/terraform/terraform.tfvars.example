# AWS Configuration
aws_region = "us-east-1"
environment = "production"
project_name = "hng13-stage6"

# EC2 Configuration
instance_type = "t3.medium"
key_name = "your-key-name"
ssh_public_key = "" # Optional: content of your SSH public key

# Security
allowed_ssh_cidr = ["0.0.0.0/0"] # Restrict this to your IP in production

# Application Configuration
domain_name = "your-domain.com"
acme_email = "your-email@example.com"
git_repo_url = "https://github.com/Clue-ess-coder/stage6-test.git"
git_branch = "main"
jwt_secret = "EverywhereFirstBlur"

# Remote State Configuration (uncomment and configure in main.tf backend block)
# terraform_state_bucket = "your-terraform-state-bucket"
# terraform_state_key = "hng13-stage6/terraform.tfstate"
# terraform_state_region = "us-east-1"
# dynamodb_table = "terraform-state-lock"
